apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mongodb-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/RispaJoseph/argocd-app-config.git
    targetRevision: HEAD
    path: mongoDB-K8s
  destination: 
    server: https://kubernetes.default.svc
    namespace: mongoDB-K8s

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
  
  


  




